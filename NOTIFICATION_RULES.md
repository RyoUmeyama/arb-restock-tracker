# 通知判定ルール仕様書

> **この文書が唯一の人間向けルール一覧。** ルールの実体は config.py / rules.py / check_stock.py に実装され、
> tests/test_logic.py がCIで毎回検証する。ルールを変更したら必ずこの文書も更新すること。
> 基本思想: **「通知が来たら本物」を守る。迷ったら通知しない側に倒す。**
> 全40項目（2026-09-09時点）。

## 1. 何を通知するか（共通・3通知経路すべてに適用）

通知経路 = ①ページ差分通知（リアルタイム） ②毎朝のチャンスダイジェスト ③RSS発見

| # | ルール | 実装 | 由来 |
|---|-------|------|------|
| 1 | 行動語（再販/入荷/抽選/予約/受付/先着/販売/在庫/整理券/受注/コラボ/限定/プロモ/発売）を含む行だけ通知 | `NOTIFY_ACTION_KEYWORDS` | PR #10,16 |
| 2 | 行動語がなくても「45日以内の将来日付＋日付以外の中身8字以上」なら通知（受付期間の更新）。ただし `strict_actions` 指定の監視ではこの救済を使わず行動語を必須にする | `_is_actionable_line` | PR #11,15 / 2026-08-19 |
| 3 | 日付だけの行（例:「2026.7.10」）は通知しない — 情報ゼロ | 同上 | PR #15 |
| 4 | 行の長さ制限は経路で異なる。**①差分通知は 8〜120字**（`rules._is_actionable_line` の定数120）、**②チャンスダイジェストは 8〜90字**（`DIGEST_LINE_MAXLEN=90`・記事紹介文の混入防止）。下限はいずれも `DIGEST_LINE_MINLEN=8`。通知本文の表示は `DIFF_LINE_MAXLEN=90` で切り詰める | `_is_actionable_line` / `extract_opportunities` | PR #14,15 |
| 5 | 定型文・ナビ断片（収録カードリスト/当たりカード/買取価格/封入確率/ストア一覧/随時更新中/速報をお届け/コチラ等）は除外。nyuka-now の状態バナー・地の文（実施中のストア/確認できています/入荷Now 等）も含む | `DIGEST_EXCLUDE_MARKERS` | PR #10,15 / 2026-08-21 |
| 6 | 状態継続文言（販売継続中等）は「変化」でないため通知しない | `STATUS_QUO_MARKERS` | PR #23 |
| 7 | ページ更新でも実質情報の行が増えていなければ通知抑制（削除だけ・定型文変化は無通知） | `_process_item` | PR #10 |
| 8 | **大会・イベントの参加抽選は通知しない**（シティリーグ/チャンピオンズリーグ/ジムバトル/トレーナーズリーグ/招待選手/先着エントリーの受付 等）。商品入手と無関係なのに「抽選」語で通過していた | `DIGEST_EXCLUDE_MARKERS` | 2026-08-31 |
| 9 | **広告行は通知しない**（【広告】/オリパ/友達追加/PT購入/チャージで/初回限定）。まとめページ埋め込みのオリパ・ポイ活広告が行動語を含んで素通りしていた | `DIGEST_EXCLUDE_MARKERS` | 2026-08-31 |
| 10 | **ルール・使用可否の告知は通知しない**（使用について/使用可否/利用規約）。例:「プロモカードパックの使用についてのお知らせ」 | `DIGEST_EXCLUDE_MARKERS` | 2026-09-09 |

## 2. 商品ライフサイクル規則（ユーザーのドメイン知識・2026-07-10〜14）

| # | ルール | 実装 | 由来 |
|---|-------|------|------|
| 11 | **対象はカードゲーム商品のみ**。RSSキーワードもカード語限定（素の「ワンピース」等は使わない） | `WATCH_KEYWORDS` | PR #17 |
| 12 | サプライ類（デッキケース/スリーブ/プレイマット等）は常に通知しない | `SUPPLY_NOISE_KEYWORDS` | PR #15,16 |
| 13 | スターターセット/構築デッキ系は**ポケカのみ・初回販売（発売前後60日）だけ**通知。再販は通知しない。ポケカ以外は通知しない | `DECK_PRODUCT_KEYWORDS` `INITIAL_SALE_WINDOW_DAYS` | PR #16 |
| 14 | **例外: スタートデッキは再販でも人気** → 初回も再販も通知 | 同上（除外リスト外） | PR #16 |
| 15 | **発売から1年半（548日）超の商品は追わない**（全商品共通・再販が来ないため）。監視の自動失効＋通知行の除外＋監視候補提案の除外＋相場評価停止＋東映スイープにも適用 | `MAX_PRODUCT_AGE_DAYS` `release_date` | PR #16,17,18 |
| 16 | 非カード商品タグ（【雑誌】【書籍】【フィギュア】【ぬいぐるみ】【CD】【DVD】【ゲーム】等）は原則通知しない | `NON_CARD_CATEGORY_TAGS` | PR #23 |
| 17 | **例外: カード付録つき雑誌**（Vジャンプのプロモカード等）は通知する（プロモカード/カード付/付録カード/特典カード等で判定） | `MAGAZINE_CARD_MARKERS` | PR #23 |
| 18 | **周辺グッズ（ポケカ公式APIの `productType=peripheral`: カードホルダー・バッグ・ディスプレイフレーム等）は新商品通知から除外**。転売対象外で詳細リンクも空のため。発売カレンダー用に state への記録は続ける | `POKECARD_NOTIFY_SKIP_TYPES` `_pokecard_fresh_keys` | 2026-09-09 |

## 3. 監視項目ごとのフラグ（`WATCH_ITEMS` の任意キー）

| # | ルール | 実装 | 由来 |
|---|-------|------|------|
| 19 | `require_keywords`: 指定した語（商材名）のいずれかを含む行だけ通知。カードラボ店舗ブログ（全TCG混在）とポケセンニュース（記事本文）で使用。未指定なら従来どおり | `passes_require_keywords` | 2026-08-19 |
| 20 | `strict_actions`: 行動語を必須にし、「将来日付＋中身」だけの救済（#2）を使わない。カードラボ4店舗と動的監視（自動登録ページ）に適用 | `_is_actionable_line(strict=True)` | 2026-08-19 |
| 21 | `suppress_item_url`: 通知末尾の監視元URL行を出さない。detail に記事URLを含むポケセンニュースで、TOPページURLを重ねないために使用 | `build_messages` | 2026-08-19 |
| 22 | `no_search_fallback`: 直リンクが取れない行にストア検索URL（#28）を付けず、末尾の監視元URL（一覧）に任せる。公式info一覧のような「お知らせ」ページで、Amazon検索が無意味なため | `_process_item` | 2026-09-09 |

## 4. 事後ログ（履歴型ページ）の扱い

| # | ルール | 実装 | 由来 |
|---|-------|------|------|
| 23 | nyuka-now 集約ページは「過去の販売・再販履歴のログ」で、新規行は事後報告＝検知時点でほぼ完売している。**履歴行は直リンク先に品切れ表示（売り切れ/在庫切れ/品切れ/sold out/販売終了/入荷待ち/完売 等）が無いことを確認できた場合だけ通知**する | `_is_history_log_page` `_quick_stock_check` `SOLDOUT_MARKERS` | 2026-08-26 |
| 24 | 履歴行に確実な直リンクが無い（検索URLしか付けられない）場合は通知しない（確認手段が無いため） | 同上（「事後ログ・確認先なし」で抑制記録） | 2026-08-26 |
| 25 | 例外: 抽選/予約/受付/応募/招待を含む「これから行動できる」行は在庫確認なしで通知（締切までの猶予があるため） | `OPEN_ACTION_KEYWORDS` | 2026-08-26 |
| 26 | 簡易在庫確認は汎用文字列判定のみ（店舗別パーサは持たない）。断定できるのは「品切れ表示あり＝完売」の方向だけで、取得失敗時は未確認のまま通知する | `_quick_stock_check` | 2026-08-26 |

## 5. リンクの付け方（「通知だけで完結」）

| # | ルール | 実装 | 由来 |
|---|-------|------|------|
| 27 | 直リンクは**確実な対応が取れた場合だけ**: ①行がアンカーテキストそのもの（15字以上の完全包含） ②行内の店舗名とURLドメインが一致 | `resolve_store_link` | PR #13 |
| 28 | 確実な直リンクが無い行は「検索:」でストア検索URL（商品名クエリ・店舗名があればそのストア）。`no_search_fallback`（#22）の監視では付けない | `fallback_search_url` | PR #14 |
| 29 | アフィリエイトURLは剥がして実URL化（&amp;エンティティ復元込み）。**ストアドメイン21種**（`STORE_DOMAINS`・2026-09-09時点）以外のURLは載せない | `_unwrap_affiliate` `STORE_DOMAINS` | PR #10,11,13 |
| 30 | RSS発見は記事ページから店舗タグ一致リンクを解決して併記（記事URLも保持） | `resolve_store_link_from_article` | PR #19 |

## 6. 通知の頻度・重複制御

| # | ルール | 実装 | 由来 |
|---|-------|------|------|
| 31 | チャンスダイジェストは**新規のみ**通知（一度通知したチャンスは再通知しない。Amazon招待リクエストは一度登録すれば済むため） | `digest_seen` | PR #6(内容はPR#10相当) |
| 32 | 監視追加候補の提案も提案済みは再提案しない | `suggested_seen` | PR #8 |
| 33 | ワンピ公式ニュースは「商品情報」カテゴリ常時＋他カテゴリはコラボ/抽選等のみ＋大会系語（バトル/大会/交流会等）は常に除外 | `ONEPIECE_NEWS_*` | PR #16,18 |
| 34 | ポケカ/ワンピ新商品検知からサプライ類タイトルを除外 | 各検知ブランチ | PR #17 |
| 35 | 東映在庫スイープ: ×→入荷の変化のみ通知（販売終了等は除外）。1年半超の旧弾はログのみ（`TOEI_SWEEP_OLD_ALERT=True`で通知化可） | `_sweep_restocked` | PR #14,18 |
| 36 | **発売カレンダーは節目再通知**: これから発売する商品（ポケカ公式API・120日以内・3,000円以上または拡張パック系）を、発売までの残日数が 60/30/14/7/3/1 日の節目をまたぐたびに再通知する。一度きりだと応募が始まる時期に何も知らせない（30th抽選 8/10〜8/14 の取りこぼしが実害） | `report_release_calendar` `RELEASE_MILESTONES` | 2026-08-19 |

## 7. 運用系

| # | ルール | 実装 | 由来 |
|---|-------|------|------|
| 37 | 毎朝9時台に日次ヘルスレポート（監視N件・内訳つき）。届かない日は異常。毎週月曜は週次サマリ（通知/ノイズ抑制/新チャンス件数）を同梱し、フィルタの絞りすぎを数字で監視 | `append_heartbeat` `weekly_stats` | PR #3,12,20 |
| 38 | **フィルタが抑制した行は理由つきで記録し、日次ヘルスレポートで直近24h分（最大8件表示）を開示する**（「送信前に精度を確認しつつ、送り忘れも逃さない」）。抑制理由: 実質情報フィルタ／対象キーワードなし／事後ログ・確認先なし／リンク先が品切れ表示 | `_log_suppression` `SUPPRESS_LOG_KEEP`（200件保持） | 2026-08-31 |
| 39 | **自動発見した抽選まとめページは監視に自動追加する（動的監視）**: anime-matsuri の「reservation-lottery」ページを毎朝発見し、未監視なら `stock_state.json` の `auto_watch` に登録して次パスから page_update で監視（`strict_actions` 付き）。上限40件・古いものからFIFO。`WATCH_ITEMS` と重複するURLは登録しない（手動定義優先）。発見通知には「自動で監視に追加済み」と明記 | `discover_am_lottery_pages` `register_auto_watch` `auto_watch_items` `AUTO_WATCH_ENABLED/MAX` | PR #25 / 2026-08-19 |
| 40 | Actions実行失敗は即Discord通知。通知はメール＋Discordの2経路固定（新しい通知アプリは追加しない・ユーザー方針） | notify-failure.yml / — | PR #7 / ntfy撤回 |

## 実装モジュール構成（2026-07-14リファクタリング後）
- `check_stock.py` — 監視の実行・状態管理・通知（オーケストレーション）。事後ログ判定・簡易在庫確認・抑制ログ・発売カレンダー・動的監視もここ
- `rules.py` — 判定規則（日付解決・実質情報・商品ライフサイクル・相場選別）
- `links.py` — リンク解決（アンカー照合・アフィ剥がし・検索URL）
- `netutil.py` — 通信（サーキットブレーカー付きHTTP）
- `config.py` — 監視対象と全ルールの定数

## 経緯の原本
各ルールの背景・実データ検証はPR #1〜#26の説明文と、2026-08以降はコミットメッセージ（PRを介さない直接コミット）に記録されている（このリポジトリのPull Requests / git log 参照）。
